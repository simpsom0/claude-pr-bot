services:
  claude-proxy:
    build:
      context: ./claude-proxy
      dockerfile: Dockerfile

  claude-datasette:
    build:
      context: ./claude-datasette
      dockerfile: Dockerfile

  claude-code:
    build:
      context: ./claude-code
      dockerfile: Dockerfile
